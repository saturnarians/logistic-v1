import { RejectAgentActionInputSchema } from '@logistics/shared'; import { rejectAgentAction } from '@logistics/core'; import { jsonError, requestBody } from '../../../../../../lib/api'; import { resolveActor } from '../../../../../../server/auth/resolve-actor';
export async function POST(request: Request, context: { params: Promise<{ id: string }> }) { const parsed = RejectAgentActionInputSchema.safeParse(await requestBody(request)); if (!parsed.success) return Response.json({ error: { code: 'VALIDATION_ERROR', message: 'Invalid rejection request' } }, { status: 400 }); try { return Response.json(await rejectAgentAction(await resolveActor(request), (await context.params).id, parsed.data.reason)); } catch (error) { return jsonError(error); } }
